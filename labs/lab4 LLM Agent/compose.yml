services:
  devui:
    image: cybersec-agent-devui
    container_name: cybersec-agent-devui
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      API_BASE_URL: "https://api.groq.com/openai/v1"
      MODEL: "openai/gpt-oss-20b"
      ENABLE_OTEL: true
      ENABLE_SENSITIVE_DATA: true             

    command: ["uv", "run", "devui", "./app", "--host", "0.0.0.0", "--port", "8080"]